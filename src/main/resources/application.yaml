server:
  port: 8080

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: -1
      max-request-size: -1
      location: /tmp/uploads
  datasource:
    url: jdbc:postgresql://localhost:5432/gallery_app_db
    username: postgres
    password: 9956788
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
